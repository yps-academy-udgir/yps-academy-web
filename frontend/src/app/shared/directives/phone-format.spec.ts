import { PhoneFormat } from './phone-format';

describe('PhoneFormat', () => {
  it('should create an instance', () => {
    const directive = new PhoneFormat();
    expect(directive).toBeTruthy();
  });
});
